import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const seed = Color(0xFF6366F1); // Indigo

  // Colores de categorías por defecto
  static const coding      = Color(0xFF6366F1);
  static const meetings    = Color(0xFF8B5CF6);
  static const communication = Color(0xFF06B6D4);
  static const design      = Color(0xFFF59E0B);
  static const browsing    = Color(0xFF10B981);
  static const social      = Color(0xFFEC4899);
  static const other       = Color(0xFF6B7280);

  // UI
  static const sidebarWidth = 220.0;
}
